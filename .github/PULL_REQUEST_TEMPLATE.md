# Pull Request

## What changed
<!-- Brief summary -->

## Test plan
- [ ] `cd backend && pytest -v`
- [ ] `cd frontend && npm test && npm run build`

## CI
All checks must pass before merge. If CI fails, fix tests locally and push again.
